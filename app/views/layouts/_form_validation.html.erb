<% if local_assigns[:item] && item.errors.any? %>
  <div id="error_explanation">
    <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
